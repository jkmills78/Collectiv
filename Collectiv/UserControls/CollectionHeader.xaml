<UserControl x:Class="Collectiv.UserControls.CollectionHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Collectiv.UserControls">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel.Style>
                <Style>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=CollectionName, Path=Visibility}" Value="Visible">
                            <Setter Property="Button.Visibility" Value="Hidden"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBox Text="{Binding Name}" Width="200"/>
            <Button Content="@" Command="{Binding ConfirmCommand}"/>
            <Button Content="!" Command="{Binding CancelCommand}"/>
        </StackPanel>
        <TextBlock x:Name="CollectionName" Text="{Binding Name}" Visibility="{Binding IsConfirmed, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
